!git --version
!git init
!git remote add origin https://github.com/maringa780/ClusToRa.git
!git remote -v
%%
fid = fopen('.gitignore','w');
fprintf(fid,'*.txt\n');
fprintf(fid,'data/\n');
fclose(fid);
%%
!git add .
!git commit -m "Initial commit"
!git branch -M main
!git push -u origin main

%%% If you pdate online files
!git pull origin main

%%% Future updates
!git status
!git add .
!git commit -m "GUI style update"
!git push










!git clone https://github.com/maringa780/ClusToRa.git